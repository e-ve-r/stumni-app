<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Student Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">

  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f8f9fa;
    }
    .sidebar {
      min-height: 100vh;
      background-color: #0A2540;
      color: white;
    }
    .sidebar .nav-link {
      color: white;
      font-weight: 500;
      margin: 5px 0;
    }
    .sidebar .nav-link:hover,
    .sidebar .nav-link.active {
      background-color: #FFC107;
      color: #0A2540;
      border-radius: 5px;
    }
    .content {
      padding: 20px;
    }
    .section {
      display: none; /* hide all by default */
    }
    .section.active {
      display: block; /* show only active */
    }
  </style>
</head>
<body>

<div class="container-fluid">
  <div class="row">
    <!-- Sidebar -->
    <nav class="col-md-3 col-lg-2 d-md-block sidebar collapse p-3">
      <h3 class="text-center mb-4">Dashboard</h3>
      <ul class="nav flex-column">
        <li class="nav-item">
          <a class="nav-link active" data-target="overview"><i class="bi bi-house me-2"></i> Overview</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" data-target="mentors"><i class="bi bi-people me-2"></i> Mentors</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" data-target="projects"><i class="bi bi-kanban me-2"></i> Projects</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" data-target="events"><i class="bi bi-calendar-event me-2"></i> Events</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/"><i class="bi bi-box-arrow-left me-2"></i> Logout</a>
        </li>
      </ul>
    </nav>

    <!-- Main content -->
    <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 content">
      <div id="overview" class="section active">
        <h2>Welcome, <%= text %> 👋</h2>
        <p>This is your personal dashboard. Navigate through the sidebar to explore.</p>
      </div>

      <div id="mentors" class="section">
        <h2>Your Mentors</h2>
        <p>Find and connect with alumni mentors here.</p>
      </div>

      <div id="projects" class="section">
        <h2>Projects</h2>
        <p>Work on exciting projects with your peers and alumni.</p>
      </div>

      <div id="events" class="section">
        <h2>Upcoming Events</h2>
        <p>Stay tuned for upcoming workshops, seminars, and networking events.</p>
      </div>
    </main>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
  // Sidebar click to show/hide sections
  document.querySelectorAll('.sidebar .nav-link[data-target]').forEach(link => {
    link.addEventListener('click', function () {
      // remove active class from all links
      document.querySelectorAll('.sidebar .nav-link').forEach(el => el.classList.remove('active'));
      this.classList.add('active');

      // hide all sections
      document.querySelectorAll('.section').forEach(sec => sec.classList.remove('active'));

      // show selected section
      const targetId = this.getAttribute('data-target');
      document.getElementById(targetId).classList.add('active');
    });
  });
</script>

</body>
</html>
